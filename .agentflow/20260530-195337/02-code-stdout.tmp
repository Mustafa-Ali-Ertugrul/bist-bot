scan_logs tool placeholder
pytest_changed tool placeholder
market_data_health tool placeholder
config_audit tool placeholder
Completed the stage.

## Files changed
- No files changed by me.

## Behavior changed
- No code change was needed.
- Registration is already present:
  - Streamlit UI has `Giriş / Kayıt Ol` tabs in `src/bist_bot/streamlit_app.py`.
  - Register form posts to `/api/auth/register`.
  - Flask backend has `/api/auth/register` in `src/bist_bot/dashboard.py`.
  - Current local `.env` already has `ALLOW_PUBLIC_REGISTRATION=true`.

## Commands run and results
- `git status --short`
  - Shows many pre-existing modified/untracked files; I did not alter them.
- `pytest tests/test_auth.py -k "register or login_successful"`
  - Result: **2 passed, 3 failed**
  - Failures are assertion mismatches caused by Turkish locale text now using proper Turkish characters, while tests still expect ASCII strings.
- `ruff check .`
  - Failed only on pre-existing untracked `get-pip.py` lint issues.

## Blockers / test gaps
- Registration itself appears implemented and enabled locally.
- Targeted auth tests are blocked by stale expected locale strings in `tests/test_auth.py`, not by registration logic.
- Full lint is blocked by untracked `get-pip.py`.
