# Local / stack observability provisioning for Grafana (optional).
# Datasource is provisioned so Prometheus is auto-linked.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
